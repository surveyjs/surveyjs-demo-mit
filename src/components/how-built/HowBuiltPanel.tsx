"use client";

/**
 * The "How this page is built" panel — a stub.
 *
 * The top bar's toggle already flips `useHowBuilt().open`; drawing the panel for
 * it is a separate task. Until then this renders nothing, so the toggle changes
 * no pixels.
 */
export function HowBuiltPanel() {
  return null;
}
